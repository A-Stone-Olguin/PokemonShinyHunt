[project]
name = "shiny-hunt"
version = "0.1.0"
description = "Automated shiny Pokemon hunting"
requires-python = ">=3.11"
dependencies = [
    "opencv-python",
    "numpy",
    "future",
    "iso8601",
    "PyYAML",
    "serial",
]

[project.scripts]
shiny-hunt = "shiny_hunt.main:main"