[project]
name = "shiny-pokemon"
version = "0.1.0"
description = "Automated shiny Pokemon hunting"
requires-python = ">=3.11"
dependencies = [
    "opencv-python",
    "numpy",
]

[project.scripts]
shiny-pokemon = "shiny_pokemon.main:main"